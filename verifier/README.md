# Verifier index

- **v1** (2026-07-21): `v1/check.py` — verifica existencia de archivos requeridos, sintaxis AST de todo el código Python, variables clave en `.env.example` y secciones mínimas del README. Primer verificador del proyecto.
- **v2** (2026-07-21): `v2/check.py` — todo lo de v1 + existencia de `src/agent/images.py` (nano banana / Gemini image), nodo `image_node` en el grafo, `media_upload` en el cliente de X, `google-genai` en requirements y variables `ENABLE_IMAGES`/`GOOGLE_API_KEY`/`IMAGE_MODEL` en `.env.example`. Añadido tras integrar la generación de imágenes.
- **v3** (2026-07-21): `v3/check.py` — todo lo de v2 + `x_research.py` (lectura de cuentas de X con Bearer Token y `X_ACCOUNTS`), `timing.py` (record/refresh/analyze de horarios con LLM), aristas del grafo en orden research→select→image→draft, scheduler dinámico en `main.py` y registro de publicaciones en `approval.py`. Añadido tras la ronda de "investigar en X + proactividad + mejores horarios".
- **v4** (2026-07-21): `v4/check.py` — todo lo de v3 + `llm.py` multi-provider (Gemini por defecto, roles cheap/draft), `growth.py` (discover/follow/prune con topes y whitelist, draft_replies), manejo de respuestas aprobadas en `approval.py`, `x_client.reply`, jobs de crecimiento en `main.py` y nuevas dependencias langchain-google-genai/anthropic. Añadido tras la ronda de "LLM por tarea + seguir/dejar de seguir/comentar".
- **v5** (2026-07-21): `v5/check.py` — esencia de v4 + topes al máximo seguro: `FOLLOW_PER_DAY=100` con `FOLLOW_BATCH=25` en 4 tandas (8/12/16/20h), pacing aleatorio 30-60s entre follows/unfollows, `UNFOLLOW_PER_WEEK=50` con `UNFOLLOW_BATCH=7` y prune diario. Añadido tras la ronda de "subir topes al límite seguro verificado contra la doc oficial de X".
